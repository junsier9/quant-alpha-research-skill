# One Python regex per line. Each pattern is matched against:
# "<rel_path>:<pattern_name>:<offending_line>"
#
# Each entry must be paired with a justification comment immediately above it.
#
# Example, commented out:
# # docs/credentials_example.md fake key in fenced code block, line 42
# ^docs/credentials_example\.md:openai_api_key:
